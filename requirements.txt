# Runtime dependencies for the training pipeline + prediction API.
# (Trimmed from the original notebook/dev environment freeze, which included
# Jupyter/plotting tooling not needed to run or containerize the app.)
pandas==3.0.3
numpy==2.5.1
scikit-learn==1.9.0
scipy==1.18.0
joblib==1.5.3
flask==3.1.0
gunicorn==23.0.0
